import React, { useState } from "react";
import SideCard from "../molecules/SideCard";

const RightSection = () => {
  const coba = () => {};

  return (
    <div className="absolute md:top-[10%] top-[28%] md:right-[18%] right-4 z-50">
      <SideCard />
    </div>
  );
};

export default RightSection;
