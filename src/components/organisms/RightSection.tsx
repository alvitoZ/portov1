import React, { useState } from "react";
import SideCard from "../molecules/SideCard";

const RightSection = () => {
  const coba = () => {};

  return (
    <div className="absolute md:top-[10%] top-[30%] 2xl:right-[35vw] md:right-[20vw] right-4 z-50">
      <SideCard />
    </div>
  );
};

export default RightSection;
